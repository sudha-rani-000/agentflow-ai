import AppShell from '../components/AppShell';
import { Check, KeyRound, LockKeyhole, UserRound } from 'lucide-react';
export default function Settings() { return <AppShell active="settings"><main className="dashboard-page"><div className="page-heading"><div><p className="eyebrow">Workspace preferences</p><h1>Settings.</h1><p className="muted">Manage your operator profile and platform security.</p></div></div><div className="settings-grid"><section className="settings-card"><div className="settings-card-head"><UserRound size={18} /><div><h2>Profile</h2><p>Your workspace identity</p></div></div><label>Name<input defaultValue="Alex Morgan" /></label><label>Email<input defaultValue="alex@company.com" /></label><button className="primary-button">Save changes</button></section><section className="settings-card"><div className="settings-card-head"><LockKeyhole size={18} /><div><h2>Security health</h2><p>Environment configuration</p></div></div>{[['JWT session signing', 'Configured'], ['Credential encryption', 'Configured'], ['Database persistence', 'In-memory fallback']].map(([name, value]) => <div className="health-row" key={name}><span><Check size={14} />{name}</span><small>{value}</small></div>)}</section></div></main></AppShell>; }

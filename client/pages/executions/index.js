import AppShell from '../../components/AppShell';
import { Activity, ArrowUpRight, CheckCircle2, Clock3 } from 'lucide-react';

const runs = [{ name: 'Invoice routing', status: 'completed', time: '2 min ago', duration: '18.4s', agent: 'Monitoring' }, { name: 'New lead alert', status: 'completed', time: '41 min ago', duration: '8.2s', agent: 'Monitoring' }, { name: 'Invoice routing', status: 'running', time: 'Now', duration: '12.7s', agent: 'Execution' }];
export default function Executions() { return <AppShell active="executions"><main className="dashboard-page"><div className="page-heading"><div><p className="eyebrow">Execution history</p><h1>Runs in motion.</h1><p className="muted">A complete, auditable timeline of every workflow execution.</p></div></div><div className="execution-list">{runs.map((run, index) => <div className="execution-row" key={`${run.name}-${index}`}><span className={`execution-icon ${run.status}`}><Activity size={17} /></span><div className="workflow-name"><strong>{run.name}</strong><small><span className="agent-pill">{run.agent}</span> {run.time}</small></div><span className={`status ${run.status === 'running' ? 'active' : 'completed'}`}>{run.status}</span><span className="row-stat"><Clock3 size={13} /> {run.duration}</span><ArrowUpRight size={16} /></div>)}</div><div className="timeline-preview"><div className="section-heading"><h2>Live agent timeline</h2><span className="live-badge"><i /> Live</span></div>{['Planner mapped 3 nodes into execution order.', 'Execution agent completed Gmail action.', 'Validation passed required output fields.'].map((event, index) => <div className="timeline-event" key={event}><span>{index + 1}</span><div><b>{['Planner', 'Execution', 'Validation'][index]}</b><p>{event}</p></div><small>{index + 1}s ago</small></div>)}</div></main></AppShell>; }

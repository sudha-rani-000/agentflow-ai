import AppShell from '../components/AppShell';
import { Check, ChevronRight, Mail, MessageSquare, Sheet, Waves } from 'lucide-react';
const providers = [{ name: 'Gmail', key: 'gmail', copy: 'Send and read email', icon: Mail }, { name: 'Slack', key: 'slack', copy: 'Messages and event alerts', icon: MessageSquare }, { name: 'Discord', key: 'discord', copy: 'Post bot messages', icon: Waves }, { name: 'Google Sheets', key: 'google-sheets', copy: 'Append rows and read ranges', icon: Sheet }];
export default function Integrations() { return <AppShell active="integrations"><main className="dashboard-page"><div className="page-heading"><div><p className="eyebrow">Connected services</p><h1>Your integrations.</h1><p className="muted">Give your workflows the tools they need to act.</p></div></div><div className="integration-list">{providers.map(({ name, key, copy, icon: Icon }) => <div className="integration-row" key={key}><span className="integration-icon"><Icon size={20} /></span><div><strong>{name}</strong><small>{copy}</small></div><span className="integration-state"><i /> Not connected</span><button className="secondary-button">Connect <ChevronRight size={15} /></button></div>)}</div><div className="security-note"><Check size={17} /><div><strong>Credentials are protected</strong><p>Access and refresh tokens are encrypted at rest. Missing credentials surface explicitly in the execution timeline.</p></div></div></main></AppShell>; }
